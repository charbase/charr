% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-backend-core.R
\name{charr_threads}
\alias{charr_threads}
\title{Set the number of worker threads}
\usage{
charr_threads(value = NULL)
}
\arguments{
\item{value}{\code{NULL} to query the current count, or a positive whole number
of threads. \code{\link[parallel:detectCores]{parallel::detectCores()}} is the usual source of an upper
bound; note that CRAN limits checks to two cores.}
}
\value{
The current count when querying. When setting, the previous count
is returned invisibly.
}
\description{
\code{charr_threads()} sets how many threads charr's \code{"altrep"} backend may use
for the operations that have a data-parallel shape. Operations without that
shape, and the \code{"base"} and \code{"stringi"} backends, always run on one thread.
}
\details{
An eligible operation uses the requested count, capped by its number of
tasks and an internal safety limit of 256. Charr does not apply an
input-size heuristic, so even a small eligible input starts workers when the
count is greater than one. A few modes must stay serial because their
results depend on the order in which elements run.

The count defaults to \code{1}, so charr is single-threaded until asked
otherwise. It is held in native code rather than in an R option, because
threading happens only in native code. This accessor is the only way to
change it: setting an option named \code{charr_threads} has no effect.
}
\examples{
old <- charr_threads(2)
charr_threads()
charr_threads(old)
}
