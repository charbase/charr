% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-backend-core.R
\name{charr_chunking}
\alias{charr_chunking}
\alias{charr_chunks_per_worker}
\alias{charr_min_chunk}
\title{Tune how parallel work is divided}
\usage{
charr_chunks_per_worker(value = NULL)

charr_min_chunk(value = NULL)
}
\arguments{
\item{value}{\code{NULL} to query the current setting, or a positive whole
number.}
}
\value{
The current setting when querying. When setting, the previous value
is returned invisibly.
}
\description{
These control how a data-parallel operation cuts its work into chunks once
\code{\link[=charr_threads]{charr_threads()}} has decided how many threads to use. Neither affects
whether threads are used at all, and neither changes any result.
}
\details{
Threads draw chunks one at a time rather than taking a fixed slice each, so
a thread that finishes a cheap chunk comes back for another. That is what
keeps uneven input balanced: element cost is not uniform, and one long
string can outweigh a thousand short ones.

\code{charr_chunks_per_worker()} sets how many chunks each thread should get,
defaulting to \code{128}. Higher values balance skewed input better and cost a
little more bookkeeping. \code{charr_min_chunk()} sets the smallest number of
elements a chunk may hold, defaulting to \code{256}, which stops a large thread
count from cutting the work finer than it is worth handing out.

With \code{n} elements and \code{w} threads the chunk size is
\code{ceiling(n / (w * chunks_per_worker))}, raised to at least \code{min_chunk} and
then lowered if needed so that there are never fewer chunks than threads. A
short vector therefore still spreads across every thread.

Like \code{\link[=charr_threads]{charr_threads()}}, both settings are held in native code rather than in
an R option, because they are only ever read there. These accessors are the
only way to change them.
}
\examples{
old <- charr_chunks_per_worker(256)
charr_chunks_per_worker()
charr_chunks_per_worker(old)
}
