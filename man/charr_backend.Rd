% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-backend-core.R
\name{charr_backend}
\alias{charr_backend}
\title{Select charr's string backend}
\usage{
charr_backend(value = NULL)
}
\arguments{
\item{value}{\code{NULL} to query the current backend, or one of \code{"stringi"},
\code{"base"}, or \code{"altrep"} to select a backend.}
}
\value{
The current backend when querying. When setting, the previous
backend is returned invisibly.
}
\description{
\code{charr_backend()} selects the implementation used by charr's public string
functions. Dispatch happens once, in R, when a public operation starts.
Nested calls stay within the selected backend.
}
\details{
The available backends are:
\itemize{
\item \code{"stringi"}: the original stringr route through installed stringi.
\item \code{"base"}: charr's optimized backend returning ordinary R character
vectors.
\item \code{"altrep"}: charr's optimized charport backend returning ALTREP character
vectors where appropriate. This is the default.
}

The selection is stored in the global \code{charr_backend} option. Changing the
option affects later public calls; it does not change an operation already
in progress.
}
\examples{
old <- charr_backend("base")
charr_backend()
charr_backend(old)
}
